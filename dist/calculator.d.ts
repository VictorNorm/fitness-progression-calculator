import type { ExerciseData, ProgramType, UserEquipmentSettings, ProgressionResult } from './types';
export declare function calculateProgression(data: ExerciseData, programType: ProgramType, settings?: UserEquipmentSettings): ProgressionResult;
